# VirVentures Returnability Engine
# Only list packages not already bundled by Streamlit or gspread.
# pandas, requests, altair, numpy, etc. are Streamlit dependencies — omit them.
python-dotenv>=1.0.1
gspread>=6.0
google-auth>=2.0
google-auth-oauthlib>=1.0
openpyxl>=3.1
streamlit>=1.35.0
